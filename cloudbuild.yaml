steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - hello_get
  - --region=europe-west1
  - --source=api_function
  - --trigger-http
  - --runtime=python312
  - --gen2 